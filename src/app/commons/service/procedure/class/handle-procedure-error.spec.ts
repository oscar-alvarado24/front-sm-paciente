import { HandleProcedureError } from './handle-procedure-error';

describe('HandleProcedureError', () => {
  it('should create an instance', () => {
    expect(new HandleProcedureError()).toBeTruthy();
  });
});
