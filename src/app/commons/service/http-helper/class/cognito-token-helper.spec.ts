import { CognitoTokenHelper } from './cognito-token-helper';

describe('CognitoTokenHelper', () => {
  it('should create an instance', () => {
    expect(new CognitoTokenHelper()).toBeTruthy();
  });
});
